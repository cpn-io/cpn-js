## RQ-009. Any newly created place has default labels
When a new place is created, it has the labels for colorset and initial marking with default values: 

colorset = UNIT

initial marking = "" (empty)
